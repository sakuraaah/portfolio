services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - '5174:5173'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      CHOKIDAR_USEPOLLING: 'true'
